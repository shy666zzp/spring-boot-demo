server:
  port: 8080
  servlet:
    context-path: /demo
spring:
  redis:
    host: 127.0.0.1  # Redis 单机节点地址
    port: 6379       # Redis 单机节点端口
    password: 123456  # 密码（若无密码则删除此行）
    database: 0       # 数据库索引（默认0）